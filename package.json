{
  "private": true,
  "name": "modifier",
  "workspaces": [
    "packages/app",
    "packages/server"
  ],
  "dependencies": {},
  "scripts": {
    "server:dev": "yarn workspace @modifier/server dev ",
    "server:lint": "yarn workspace @modifier/server lint",
    "app:dev": "yarn workspace @modifier/app dev ",
    "app:lint": "yarn workspace @modifier/app build "
  }
}
